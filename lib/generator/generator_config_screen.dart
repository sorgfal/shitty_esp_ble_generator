import 'package:flutter/material.dart';

class GeneratorConfigScreen extends StatelessWidget {
  const GeneratorConfigScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}
