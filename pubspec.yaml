name: shitty_esp_ble_generator
description: Генератор BLE прошивки для ESP32 в ArduinoIde
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.6.0+12

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  uuid: ^3.0.5
  archive: ^3.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/base.tmpl